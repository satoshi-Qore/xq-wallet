# components/ui/

Primitive, reusable UI components with no business logic.
Each component should be:
- Fully typed with explicit props interface
- Self-contained (styles live with the component)
- Documented with a JSDoc comment

Examples: Button, Input, Badge, Card, Modal, Tooltip, Spinner
